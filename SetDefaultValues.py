# The setdefault() method in dictionaries allows you to set a default value if the key doesn’t exist.

data = {"name": "Alice"}
data.setdefault("age", 30)
print(data)  # Output: {'name': 'Alice', 'age': 30}
